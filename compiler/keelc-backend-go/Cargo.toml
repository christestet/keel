[package]
name = "keelc-backend-go"
version = "0.1.0"
edition = "2021"
description = "M3 Go source emitter for keelc."

[dependencies]
keelc-kir = { path = "../keelc-kir" }
keelc-types = { path = "../keelc-types" }
