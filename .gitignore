/target
Cargo.lock.orig

# macOS
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride

# Conformance build-mode artifacts (compiled test binaries)
tests/conformance/*/main

# Agent-harness runtime state (per-session lock file, not repo content)
.agents/scheduled_tasks.lock
