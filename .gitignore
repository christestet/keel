/target
Cargo.lock.orig

# macOS
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride

# Conformance build-mode artifacts (compiled test binaries + build-cache stamps)
tests/conformance/*/main
tests/conformance/*/.main.keelstamp

# Agent-harness runtime state (per-session lock file, not repo content)
.agents/scheduled_tasks.lock
