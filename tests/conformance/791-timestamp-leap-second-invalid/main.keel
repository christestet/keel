use std.json

fn quote() -> Char { '"' }

fn main() {
    let timestamp = json.parse<Timestamp>("{quote()}2016-12-31T23:59:60Z{quote()}") catch err {
        TypeMismatch(_, _) => return print("invalid"),
        other => return print("wrong error"),
    }
    print(timestamp)
}
