use std.json

struct User {
    name: String
    active: Bool
}

fn main() {
    let encoded = json.write(User { name: "Ada", active: true }) catch err {
        other => return print("error"),
    }
    print(encoded)
}
