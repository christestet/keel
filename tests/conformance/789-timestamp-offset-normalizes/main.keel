use std.json

fn quote() -> Char { '"' }

fn main() {
    let encoded = json.write(
        json.parse<Timestamp>("{quote()}2026-06-20T14:34:56+02:00{quote()}") catch err {
            other => return print("parse error"),
        },
    )
    print(encoded)
}
