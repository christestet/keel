use std.json

struct User {
    name: String
}

fn quote() -> Char { '"' }

fn main() {
    let user = json.parse<User>("{{{quote()}name{quote()}:{quote()}Ada{quote()},{quote()}extra{quote()}:1}}", mode: .tolerant) catch err {
        other => return print("error"),
    }
    print(user.name)
}
