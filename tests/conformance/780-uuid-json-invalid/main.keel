use std.json

fn quote() -> Char { '"' }

fn main() {
    let id = json.parse<Uuid>("{quote()}not-a-uuid{quote()}") catch err {
        TypeMismatch(_, _) => return print("invalid"),
        other => return print("wrong error"),
    }
    print(id)
}
