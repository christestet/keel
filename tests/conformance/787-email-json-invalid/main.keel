use std.json

fn quote() -> Char { '"' }

fn main() {
    let email = json.parse<Email>("{quote()}Ada Lovelace <ada@example.com>{quote()}") catch err {
        TypeMismatch(_, _) => return print("invalid"),
        other => return print("wrong error"),
    }
    print(email)
}
