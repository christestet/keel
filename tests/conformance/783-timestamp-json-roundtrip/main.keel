use std.json

fn quote() -> Char { '"' }

fn main() {
    let encoded = json.write(
        json.parse<Timestamp>("{quote()}2026-06-20T12:34:56.123Z{quote()}") catch err {
            other => return print("parse error"),
        },
    )
    print(encoded)
}
