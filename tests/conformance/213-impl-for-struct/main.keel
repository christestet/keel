interface Stringer {
    fn to_string(self) -> String
}

struct Point {
    x: Int
    y: Int
}

impl Stringer for Point {
    fn to_string(self) -> String {
        "({self.x}, {self.y})"
    }
}

fn main() {
    let p = Point { x: 3, y: 4 }
    print(p.to_string())
}
